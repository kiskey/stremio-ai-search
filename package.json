{
  "name": "stremio-ai-addon",
  "version": "1.0.0",
  "description": "AI-powered movie and series recommendations for Stremio",
  "main": "server.js",
  "scripts": {
    "start": "node -e \"const path=require('path');const isDev=path.basename(path.resolve()).endsWith('dev');process.env.NODE_ENV=isDev?'development':'production';require('./server.js')\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.x",
    "compression": "^1.7.4",
    "express": "^4.18.x",
    "node-fetch": "^2.7.x",
    "stremio-addon-sdk": "^1.6.x",
    "lru-cache": "^10.0.1",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "author": "ITCON",
  "license": "MIT",
  "private": true
}
